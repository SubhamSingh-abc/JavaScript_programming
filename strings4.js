let str = "Subham Singh";       
let lower_str = str.toLowerCase();
let upper_str = str.toUpperCase();
console.log(str);
console.log(upper_str);
console.log(lower_str);


// String methods don’t change the original — they return a new modified string
// str.toLowerCase();
// str.toUpperCase();